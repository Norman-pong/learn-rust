# Phase · v1.2 质量加固（1-2 周）

> 状态：已完成 · 日期：2026-07-02
>
> **上游 PRD**：[v1-2-quality](../prd/2026-07-02-v1-2-quality.md)
> **上游 Architecture**：[overview](../architecture/overview.md)（§3.3 练习格式 · §3.4 项目层）
> **前置条件**：v1.0/v1.1 已交付（93/93 pass, 20 篇内容, mdbook 可用）

---

## §1 练习重置机制（just reset）

> 溯源：← PRD §2.1

**交付物**：`just reset` 命令 + `just reset <chapter>` 单章重置

| # | 任务 | 验收标准 | 预估 |
|---|------|---------|------|
| 1.1 | 创建 `scripts/reset.rs` — 读取 exercises/src/ 每个文件，将答案逻辑替换为 `todo!()` 桩 | 脚本可执行，输出修改的文件数 | 1h |
| 1.2 | 桩格式：保留 `#[test] #[ignore]` + `// I AM NOT DONE` + assert（注释掉） | `cargo check` 通过 | 30min |
| 1.3 | justfile 添加 `reset` 和 `reset <chapter>` recipe | `just reset` / `just reset variables` 可执行 | 15min |
| 1.4 | reset 前自动 `git stash exercises/src/` | 可通过 `git stash pop` 恢复 | 15min |
| 1.5 | 验证：reset 后 `cargo test -- --ignored` ≥ 70 题 FAIL | 全量验证通过 | 15min |

---

## §2 内容代码块验证（just check-docs）

> 溯源：← PRD §2.2

**交付物**：`just check-docs` 命令 + 修复所有编译错误

| # | 任务 | 验收标准 | 预估 |
|---|------|---------|------|
| 2.1 | 创建 `scripts/check-docs.sh` — 提取 `should-compile` 标记的代码块 | 脚本可执行，输出提取的块数 | 45min |
| 2.2 | 代码块写入 exercises/tests/ 临时文件，用 exercises Cargo.toml 做依赖容器 | `cargo test --test doc_check_* --no-run` 编译通过 | 30min |
| 2.3 | justfile 添加 `check-docs` recipe | `just check-docs` 可执行，输出报告 | 15min |
| 2.4 | 给 src/ 中的完整代码块添加 `,should-compile` 标记 | ≥ 20 个代码块被标记 | 30min |
| 2.5 | 修复所有编译错误 | `just check-docs` 零 error | 30min |

---

## §3 CLI 工具实用化

> 溯源：← PRD §2.3

**交付物**：真实可用的 CLI 工具 + DEVLOG 卡点记录

| # | 任务 | 验收标准 | 预估 |
|---|------|---------|------|
| 3.1 | 确定替代目标（或选定通用工具候选） | DEVLOG.md 记录选择理由 | 15min |
| 3.2 | 创建 CLI crate（clap + serde/regex） | `cargo new` + Cargo.toml 配好依赖 | 15min |
| 3.3 | 实现核心逻辑 | `cargo run -- <args>` 输出正确 | 1.5h |
| 3.4 | 单元测试 ≥ 3 个 | 核心逻辑有测试覆盖 | 30min |
| 3.5 | DEVLOG.md 记录 ≥ 3 个真实卡点 | 每个含：症状/编译错误/理解/修复 | 30min |
| 3.6 | 在生产中替代原脚本 ≥ 1 次 | DEVLOG 记录实际调用 | — |

---

## §4 清理（warning + 死链）

> 溯源：← PRD §2.4

| # | 任务 | 验收标准 | 预估 |
|---|------|---------|------|
| 4.1 | 清理 exercises 编译 warning（`_` 前缀 + 移除 unreachable code） | `cargo test --no-run` 零 warning | 30min |
| 4.2 | 修复 mdbook 内部死链 | `mdbook build` 零 broken link 警告 | 15min |

---

## §5 验收清单

- [x] `just reset` 可一键恢复 exercises 到待做状态（≥ 70 题 FAIL）— 93 FAIL ✅
- [x] `just reset <chapter>` 支持单章重置
- [x] reset 前自动 `git stash`，可恢复
- [x] `just check-docs` 提取并编译验证 `should-compile` 代码块 — 12 块全通过
- [x] `just check-docs` 零 error
- [x] CLI 工具用到 clap + Result + IO，≥ 3 测试 — log-grep 4 tests
- [x] DEVLOG.md 含 ≥ 3 个真实卡点 — 5 条
- [x] `cargo test --no-run` 零 warning
- [x] `mdbook build` 零 broken link 警告

---

## §6 执行顺序

```
1. reset 机制（§1）           ← 最高优先，恢复学习价值
2. warning + 死链清理（§4）    ← 快速清理，30min
3. check-docs（§2）           ← 中等复杂度
4. CLI 实用化（§3）            ← 最耗时，需 Norman 确认目标
```
